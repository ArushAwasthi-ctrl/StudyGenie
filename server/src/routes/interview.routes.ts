import { Router } from "express";

const router = Router();

// POST /api/interview/start — Start mock interview session
// POST /api/interview/answer — Submit answer for evaluation + follow-up
// GET /api/interview/history — Past interview sessions

export default router;
